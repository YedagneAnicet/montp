import { FirstNameFormatPipe } from './first-name-format.pipe';

describe('FirstNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
